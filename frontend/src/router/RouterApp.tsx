import React, { FunctionComponent } from 'react';

const RouterApp: FunctionComponent = (props) => {
    return (
        <React.Fragment>
            React Router App
        </React.Fragment>
    );
}