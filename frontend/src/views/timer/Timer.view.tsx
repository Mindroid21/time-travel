import React, { FunctionComponent } from 'react';

const TimerView: FunctionComponent = () => {
    return (
        <React.Fragment>
            Timer Screen
        </React.Fragment>
    );
};

export default TimerView;